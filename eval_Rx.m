f_E = -(a.*u(:,1).*u(:,2))+ (d.*((u(:,1).*u(:,2).*a)./(d + k)))+ (k.*((u(:,1).*u(:,2).*a)./(d + k)));
f_S = -(a.*u(:,1).*u(:,2))+ (d.*((u(:,1).*u(:,2).*a)./(d + k)));
f_P = (k.*((u(:,1).*u(:,2).*a)./(d + k)));
subs__0 = 1./(d + k);
subs__1 = u(:,1).*a.*subs__0;
J_gamma_1=(u(:,2).*a)./(d + k);
J_gamma_2=(u(:,1).*a)./(d + k);
subs2__0 = (J_gamma_2.*f_S)./(J_gamma_1 + 1);
subs2__1 = -subs2__0;

Rx = [subs2__1,...
f_S,...
subs2__1 - f_S,...
subs2__0];